import sensible from '@fastify/sensible'

export const autoConfig = {}

/**
 * Provides reply.notFound(), reply.unauthorized(), httpErrors, etc.
 * @see https://github.com/fastify/fastify-sensible
 */
export default sensible
